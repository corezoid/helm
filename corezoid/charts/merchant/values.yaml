appName: merchant
image:
  registry: docker-hub.middleware.biz
  repository: merchant

postgresqlClientImage:
  registry: docker-hub.middleware.biz
  repository: postgresql-client
  tag: alpine3.18.3

port:
containers:
  containerPort: 8080
