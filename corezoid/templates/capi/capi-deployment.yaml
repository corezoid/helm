apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.capi.appName | default "capi" }}-deployment
  labels:
    app: {{ .Values.product }}
    tier: {{ .Values.capi.appName | default "capi" }}
spec:
  selector:
    matchLabels:
      tier: {{ .Values.capi.appName | default "capi" }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  replicas: {{ .Values.capi.minReplicas }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/capi/capi-configmap.yaml") . | sha256sum }}
        checksum/config_vm: {{ include (print $.Template.BasePath "/capi/capi-configmap-vm.yaml") . | sha256sum }}
        prometheus.io/path: /metrics
        prometheus.io/port: "9100"
        prometheus.io/scrape: "true"
      labels:
        app: {{ .Values.product }}
        tier: {{ .Values.capi.appName | default "capi" }}
    spec:
      {{ if .Values.capi.affinity }}
      affinity:
        {{ .Values.capi.affinity | toYaml | nindent 8 | trim }}
      {{- end}}

      terminationGracePeriodSeconds: 300

      initContainers:
        - name: init-wait

          image: docker-hub.middleware.biz/public/alpine
          command: ["sh", "-c", "until {{- if .Values.postgres.bouncer }} nc -zvw1 pgbouncer-service 5432 && {{- else }}  nc -zvw1 ${POSTGRES_DBHOST} ${POSTGRES_DBPORT} && {{- end }}  nc -zvw1  ${MQ_HOST} ${MQ_PORT}; do echo waiting for deps; sleep 2; done;"]
          env:
            - name: MQ_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.rabbitmq.secret.name }}
                  key: host
            - name: MQ_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.rabbitmq.secret.name }}
                  key: port
            - name: POSTGRES_DBHOST
              {{- if .Values.postgres.bouncer }}
              value: "pgbouncer-service"
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgres.secret.name }}
                  key: dbhost
            {{- end }}
            - name: POSTGRES_DBPORT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgres.secret.name }}
                  key: dbport
        - name: init-wait-rmq
          image: docker-hub.middleware.biz/public/alpine
          command: ['sh']
          args:
            - "-c"
            - |
              until wget -O - -q --header "Authorization: Basic `echo -n \"$MQ_USERNAME:$MQ_PASSWORD\" | base64`" http://${MQ_HOST}:15672/api/exchanges/%2Fconveyor/ | grep -qF "conf_agent_direct"; do echo "waiting for exchanges to be ready..."; sleep 2; done
          env:
            - name: MQ_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.rabbitmq.secret.name }}
                  key: host
            - name: MQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.rabbitmq.secret.name }}
                  key: username
            - name: MQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.rabbitmq.secret.name }}
                  key: password
      shareProcessNamespace: true
      containers:
        - name: {{ .Values.capi.appName | default "capi" }}
          imagePullPolicy: {{ .Values.image.imagePullPolicy | default "IfNotPresent" }}

          image: "{{ ( or .Values.capi.image.repository .Values.image.imageRegistry) }}/{{ .Values.global.repotype | default "public" }}{{ .Values.capi.image.repository | default "/conveyor_api" }}:{{ .Values.capi.tag }}"
          env:
          - name: POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          {{- if .Values.store_dumps.enabled }}
          - name: ERL_CRASH_DUMP
            value: "/var/dumps/$(POD_NAME)-erl.dump"
          {{- end }}
          - name: NODE_COOKIE
            value: {{ .Values.capi.appName | default "capi" }}-{{ .Release.Namespace }}
          - name: RELX_REPLACE_OS_VARS
            value: "true"
          - name: IPNODEDOMAIN
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
          - name: SCHEDULERS
            value: "4"
          - name: capi_front_captcha_key
            valueFrom:
              secretKeyRef:
                name: {{ .Values.capi.secret.name }}
                key: capi_front_captcha_key
          - name: capi_backend_captcha_key
            valueFrom:
              secretKeyRef:
                name: {{ .Values.capi.secret.name }}
                key: capi_backend_captcha_key
          - name: REDIS_HOST
            valueFrom:
              secretKeyRef:
                name: {{ .Values.redis.secret.name }}
                key: host
          - name: REDIS_PORT
            valueFrom:
              secretKeyRef:
                name: {{ .Values.redis.secret.name }}
                key: port
          - name: REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.redis.secret.name }}
                key: password
          - name: REDIS_HOST_CACHE
            valueFrom:
              secretKeyRef:
                name: {{ .Values.redis.secret.name }}
                key: host_cache
          - name: REDIS_PORT_CACHE
            valueFrom:
              secretKeyRef:
                name: {{ .Values.redis.secret.name }}
                key: port_cache
          - name: REDIS_PASSWORD_CACHE
            valueFrom:
              secretKeyRef:
                name: {{ .Values.redis.secret.name }}
                key: password_cache
          - name: MQ_HOST
            valueFrom:
              secretKeyRef:
                name: {{ .Values.rabbitmq.secret.name }}
                key: host
          - name: MQ_PORT
            valueFrom:
              secretKeyRef:
                name: {{ .Values.rabbitmq.secret.name }}
                key: port
          - name: MQ_VHOST
            valueFrom:
              secretKeyRef:
                name: {{ .Values.rabbitmq.secret.name }}
                key: vhost
          - name: MQ_USERNAME
            valueFrom:
              secretKeyRef:
                name: {{ .Values.rabbitmq.secret.name }}
                key: username
          - name: MQ_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.rabbitmq.secret.name }}
                key: password
          - name: MERCHANT_LOGIN
            valueFrom:
              secretKeyRef:
                name: merchant-secret
                key: merchant_login
          - name: MERCHANT_SECRET
            valueFrom:
              secretKeyRef:
                name: merchant-secret
                key: merchant_secret
          - name: ELASTICSEARCH_HOST
            valueFrom:
              secretKeyRef:
                name: {{ .Values.elasticsearch.secret.name }}
                key: host
          - name: ELASTICSEARCH_PORT
            valueFrom:
              secretKeyRef:
                name: {{ .Values.elasticsearch.secret.name }}
                key: port
          - name: POSTGRES_DBHOST
            {{- if .Values.postgres.bouncer }}
            value: "pgbouncer-service"
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: {{ .Values.postgres.secret.name }}
                key: dbhost
            {{- end }}
          - name: POSTGRES_DBUSER
            valueFrom:
              secretKeyRef:
                name: {{ .Values.postgres.secret.name }}
                key: dbuser
          - name: POSTGRES_DBPWD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.postgres.secret.name }}
                key: dbpwd
          - name: POSTGRES_DBPORT
            valueFrom:
              secretKeyRef:
                name: {{ .Values.postgres.secret.name }}
                key: dbport
          ports:
          - containerPort: {{ .Values.capi.port | default "9080" | quote }}
          - containerPort: {{ .Values.confAgentServer.port | default "8585" | quote }}
          lifecycle:
            preStop:
              exec:
                command: ["/ebsmnt/erlang/capi/bin/capi","stop"]
          volumeMounts:
            - name: {{ .Values.capi.appName | default "capi" }}config
              mountPath: /ebsmnt/conf/capi.config
              subPath: capi.config
            - name: {{ .Values.capi.appName | default "capi" }}-config-vm
              mountPath: /ebsmnt/erlang/capi/releases/1/vm.args
              subPath: vm.args
            - name: corezoid-license
              mountPath: /ebsmnt/certs
            {{- if .Values.store_dumps.enabled }}
            - name: dumps-volume
              mountPath: /var/dumps
            {{- end }}
          resources:
            {{ .Values.capi.resources | toYaml | indent 12 | trim }}
          livenessProbe:
            exec:
              command:
              - bash
              - -ec
              - |
                ulimit -n 1024
                exec /usr/bin/capi liveness
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
              - bash
              - -ec
              - |
                ulimit -n 1024
                exec /usr/bin/capi readiness
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              add: ["IPC_LOCK"]
      {{- if not  (eq .Values.global.repotype "public") }}
      imagePullSecrets:
        - name: corezoid-secret
      {{- end }}
      volumes:
        - name: {{ .Values.capi.appName | default "capi" }}config
          configMap:
            name: {{ .Values.capi.appName | default "capi" }}-config
        - name: {{ .Values.capi.appName | default "capi" }}-config-vm
          configMap:
            name: {{ .Values.capi.appName | default "capi" }}-config-vm
        - name: corezoid-license
          secret:
            secretName: license-share-new
        {{- if .Values.store_dumps.enabled }}
        - name: dumps-volume
          hostPath:
            path: /var/dumps
        {{- end }}
