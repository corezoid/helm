{{- if .Values.merchant.secret.create }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.merchant.secret.name}}
  labels:
    app: {{ .Values.global.product }}
    tier: "merchant"
type: Opaque
data:
  {{- range $key, $value := .Values.merchant.secret.data }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end}}
