{{- if .Values.elasticsearch.secret.create }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.elasticsearch.secret.name}}
  labels:
    app: {{ .Values.product }}
    tier: "elasticsearch"
type: Opaque
data:
  {{- range $key, $value := .Values.elasticsearch.secret.data }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end}}
