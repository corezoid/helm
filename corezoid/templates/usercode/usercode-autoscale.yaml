apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.usercode.appName }}-autoscale
  labels:
    app: {{ .Values.global.product }}
    tier: {{ .Values.usercode.appName }}
spec:
  minReplicas: {{ .Values.usercode.minReplicas }}
  maxReplicas: {{ .Values.usercode.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.usercode.appName }}-deployment
  targetCPUUtilizationPercentage: 70
