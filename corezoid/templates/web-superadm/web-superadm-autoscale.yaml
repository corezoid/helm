{{- if .Values.websuperadm.autoscaling -}}
{{- if .Values.websuperadm.autoscaling.enabled -}}
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "corezoid.websuperadm" . }}-hpa
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "corezoid.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.websuperadm.name }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
  {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "corezoid.websuperadm" . }}
  minReplicas: {{ .Values.websuperadm.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.websuperadm.autoscaling.maxReplicas | default 4 }}
  metrics:
    {{- if .Values.websuperadm.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageValue: {{ .Values.websuperadm.autoscaling.targetMemory }}
    {{- end }}
    {{- if .Values.websuperadm.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.websuperadm.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.websuperadm.autoscaling.requests }}
    - type: Pods
      pods:
        metric:
          name: http_requests
        target:
          type: AverageValue
          averageValue: {{ .Values.websuperadm.autoscaling.requests }}
    {{- end }}
{{- end }}
{{- end }}