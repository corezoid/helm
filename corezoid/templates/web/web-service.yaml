apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.webadm.appName }}
  labels:
    app: {{ .Values.global.product }}
    tier: {{ .Values.webadm.appName }}
spec:
  selector:
    app: {{ .Values.global.product }}
    tier: {{ .Values.webadm.appName }}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort
