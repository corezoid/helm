{{- if .Values.webadm.autoscaling -}}
{{- if .Values.webadm.autoscaling.enabled -}}
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "corezoid.webadm" . }}-hpa
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "corezoid.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.webadm.name }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
  {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "corezoid.webadm" . }}
  minReplicas: {{ .Values.webadm.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.webadm.autoscaling.maxReplicas | default 4 }}
  metrics:
    {{- if .Values.webadm.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageValue: {{ .Values.webadm.autoscaling.targetMemory }}
    {{- end }}
    {{- if .Values.webadm.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.webadm.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.webadm.autoscaling.requests }}
    - type: Pods
      pods:
        metric:
          name: http_requests
        target:
          type: AverageValue
          averageValue: {{ .Values.webadm.autoscaling.requests }}
    {{- end }}
{{- end }}
{{- end }}
