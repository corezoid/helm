{{- if eq .Values.enigma.enabled true }}
{{- if .Values.enigma.key_manager.aws_kms_role }}
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.enigma.key_manager.aws_kms_role }}
  name: enigma-kms-service
{{- end }}
  {{- end }}
