apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.mult.appName }}-autoscale
  labels:
    app: {{ .Values.global.product }}
    tier: {{ .Values.mult.appName }}
spec:
  minReplicas: {{ .Values.mult.minReplicas }}
  maxReplicas: {{ .Values.mult.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.mult.appName }}-deployment
  targetCPUUtilizationPercentage: 65
