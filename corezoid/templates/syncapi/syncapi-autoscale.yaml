apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.syncapi.appName }}-autoscale
  labels:
    app: {{ .Values.global.product }}
    tier: {{ .Values.syncapi.appName }}
spec:
  minReplicas: {{ .Values.syncapi.minReplicas }}
  maxReplicas: {{ .Values.syncapi.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.syncapi.appName }}-deployment
  targetCPUUtilizationPercentage: 60
