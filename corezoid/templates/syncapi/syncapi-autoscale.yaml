{{- if .Values.syncapi.autoscaling -}}
{{- if .Values.syncapi.autoscaling.enabled -}}
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "corezoid.syncapi" . }}-hpa
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "corezoid.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.syncapi.name }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
  {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "corezoid.syncapi" . }}
  minReplicas: {{ .Values.syncapi.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.syncapi.autoscaling.maxReplicas | default 4 }}
  metrics:
    {{- if .Values.syncapi.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageValue: {{ .Values.syncapi.autoscaling.targetMemory }}
    {{- end }}
    {{- if .Values.syncapi.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.syncapi.autoscaling.targetCPU }}
    {{- end }}
    {{- if .Values.syncapi.autoscaling.requests }}
    - type: Pods
      pods:
        metric:
          name: http_requests
        target:
          type: AverageValue
          averageValue: {{ .Values.syncapi.autoscaling.requests }}
  {{- end }}
{{- end }}
{{- end }}