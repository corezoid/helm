{{- if eq .Values.global.storage "manual" }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.mult.appName }}-{{ .Values.global.storage}}-{{ .Release.Name }}
  labels:
    tier: {{ .Values.mult.appName }}
spec:
  storageClassName: {{ .Values.global.storageClassName }}
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Delete
  hostPath:
    path: /ebsmnt/{{ .Release.Name }}/{{ .Values.mult.appName }}
{{- end }}
