apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mult.appName }}-service-node
  labels:
    app: {{ .Values.global.product }}
    tier: {{ .Values.mult.appName }}
spec:
  selector:
    app: {{ .Values.global.product }}
    tier: {{ .Values.mult.appName }}
  ports:
  - protocol: TCP
    port: 9082
    targetPort: 9082
  type: NodePort
